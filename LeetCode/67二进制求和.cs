namespace LeetCode
{
    public class _67二进制求和 {
        public class Solution {
            public string AddBinary(string a, string b) {
        
                char[] chars = new char[a.Length + b.Length];
                char[] chars2 = new char[(a.Length + b.Length)*2];

                
                while (true)
                {
                    
                }
            }

            public void Sum(char[] a, char[] b,char[] chars, char[] chars2)
            {
                int i = 0;
                while (true)
                {
                    var tmpa1 = a.Length - 1 - i >= 0 && a.Length - 1 - i < a.Length ? a[a.Length - 1 - i] : 0;
                    i++;
                }
            }
        }
    }
}